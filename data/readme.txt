aliases.json: mapping from short names to long names and long names to short names to help standardize the name format later

GES_missions.json: missions that GES DISC serves

GES_instruments.json: instruments GES DISC serves

variables.json: List of variable that are important

mission_instrument_couples_LOWER.json: dictionary mapping mission to list of all instruments on that mission. (Used to find valid couples later)

models_and_analyses_LOWER.json: list of datasets that are of type 'reanalysis' meaning they do not have instruments and variables. ie: MERRA
